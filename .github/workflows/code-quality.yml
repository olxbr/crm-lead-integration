name: Unit Tests

on:
  pull_request:
    branches: master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run unit tests
        run: make test

      - name: Quality Gate - Unit Test
        run: true